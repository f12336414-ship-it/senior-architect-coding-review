# 需求未就绪时停止设计

请求：把支付失败订单自动重试，保证成功。

阻塞问题：

- 支付提供方在超时后是否可能实际成功？
- 每个支付意图是否有稳定幂等键？
- 哪些失败码可重试，最大时间和次数是什么？
- 重复扣款由谁承担，如何检测和补偿？

反例：提供方已扣款但响应丢失，系统把它视为失败并再次创建支付，造成重复扣款。

门禁：`NOT_READY`。在确认幂等协议、未知提交结果恢复和业务风险接受者之前，不输出“加重试队列”的正式架构，也不修改支付代码。
